<div>
  <%= form_with model: @material_relation, local: true do |f| %>
    <div>
      生徒選択
      <%= f.collection_select :material_relation, Student.all, :id, :first_name, prompt: '選択してください' %>
    </div>
    <div>
      <%= f.submit '記入完了' %>
    </div>
  <% end %>
</div>